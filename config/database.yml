development:
     adapter: mysql2
     database: election-dev
     username: root
     password: root
     socket: /Applications/MAMP/tmp/mysql/mysql.sock
     pool: 5
     timeout: 5000
#:username, :password, :host, :port, :database, :socket, :flags
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
     adapter: mysql2
     database: election-test
     username: root
     password: root
     socket: /Applications/MAMP/tmp/mysql/mysql.sock
     pool: 5
     timeout: 5000

production:
     adapter:
     database:
     pool: 5
     timeout: 5000
